replace J(N)E by J(N)Z (JE is supposed to be not supported in 64bits)

add randomness in equal score moves

ban impossible moves

handle equality scenarios

use XOR al, al instead of MOV al, 0 (2 bytes in memory instead of 5)
